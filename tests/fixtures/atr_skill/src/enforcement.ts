// Excerpt of src/enforcement.ts at ATR faf743fee8a5018467959ec8ea7ccdb1a1aab333, MIT licensed.
// Only the spans agent_defs.loaders.atr_skill_gates reads are kept, so this fixture cannot drift into a rewrite of upstream's logic.

// --- the default detection lane: src/enforcement.ts lines 106-106 ---
export const DEFAULT_LANE: Lane = 'hunt';
